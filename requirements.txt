# Targets Kaggle pinned env: torch 2.10 (CUDA 12.8, provided by platform, do NOT pin),
# transformers==5.0.0, bitsandbytes (latest, install with --no-deps).
transformers==5.0.0
bitsandbytes
# peft required for LoRA fine-tuning in all experiments; install with --no-deps on Kaggle pinned env to avoid disturbing torch/transformers.
peft>=0.13
trl==1.8.0
datasets==2.21.0
accelerate==0.33.0
pandas
matplotlib
tqdm
scikit-learn
huggingface_hub
